<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".recorrido">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!--Titulo de la vista, para diferenciarlo-->
        <TextView
            android:id="@+id/txt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="RECORRIDO" />

        <!-- Espacio donde se coloca de donde el lugar
            de donde parte el camion con el producto -->
        <TextView
            android:id="@+id/txtOrigen1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:editable="true" />

        <!-- Espacio donde se coloca de donde el lugar
            de donde llega el camion con el producto -->
        <TextView
            android:id="@+id/txtDestino1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

         <!-- Espacio donde se coloca de donde el lugar
            de donde parte el camion con el producto -->
        <!-- se lo realizo para mostrar dos registros distintos-->
        <TextView
            android:id="@+id/txtOrigen2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Espacio donde se coloca de donde el lugar
            de donde llega el camion con el producto -->
        <!-- se lo realizo para mostrar dos registros distintos-->
        <TextView
            android:id="@+id/txtDestino2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>
</android.support.constraint.ConstraintLayout>
